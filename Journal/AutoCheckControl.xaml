<UserControl x:Class="TNovCommon.AutoCheckControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:TNovCommon"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=local:AutoCheckControlViewModel}"
             Background="Transparent">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <local:BooleanToStrikeConverter x:Key="BooleanToStrikeConverter"/>
            <local:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Автоматические проверки"
                       Style="{StaticResource PanelHeaderStyle}"
                       HorizontalAlignment="Center"
                       FontSize="14"
                       Margin="0,0,0,8"/>
            <Button Content="Обновить проверки"
                    Command="{Binding UpdCommand}"
                    Style="{StaticResource SecondaryButtonStyle}"
                    HorizontalAlignment="Stretch"
                    Padding="14,8"/>
        </StackPanel>

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    Margin="0,0,0,10">
            <TextBlock Text="Статус:"
                       Foreground="{StaticResource MutedBrush}"
                       FontSize="12"
                       VerticalAlignment="Center"
                       Margin="0,0,8,0"/>
            <ComboBox ItemsSource="{Binding Statuses}"
                      SelectedItem="{Binding SelectedStatus}"
                      Style="{StaticResource DarkComboBoxStyle}"
                      MinWidth="180"
                      Margin="0"/>
        </StackPanel>

        <Border Grid.Row="2"
                BorderBrush="{StaticResource SelectedBrush}"
                BorderThickness="0,1,0,0"
                Padding="0,8,0,0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding ItemsView}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource ChangeCardStyle}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <CheckBox IsChecked="{Binding IsChecked}"
                                              IsEnabled="False"
                                              Style="{StaticResource DarkCheckBoxStyle}"
                                              VerticalAlignment="Center"
                                              Margin="0,0,8,0">
                                        <CheckBox.LayoutTransform>
                                            <ScaleTransform ScaleX="1.2" ScaleY="1.2"/>
                                        </CheckBox.LayoutTransform>
                                    </CheckBox>

                                    <StackPanel Grid.Column="1">
                                        <TextBlock Text="{Binding Title}"
                                                   TextWrapping="Wrap"
                                                   Style="{StaticResource JournalModuleStyle}"/>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding DisplayDate}"
                                                       Style="{StaticResource JournalDateStyle}"
                                                       Margin="0,3,0,0"/>
                                            <TextBlock Text="{Binding Creator}"
                                                       Style="{StaticResource JournalDateStyle}"
                                                       Margin="8,3,0,0"/>
                                        </StackPanel>
                                    </StackPanel>

                                    <StackPanel Grid.Column="2"
                                                Orientation="Horizontal"
                                                VerticalAlignment="Center">
                                        <Button Content="Выбрать элементы"
                                                Command="{Binding DataContext.SelectElemsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"
                                                Style="{StaticResource SecondaryButtonStyle}"
                                                Padding="10,6"
                                                Margin="0,0,4,0"
                                                Visibility="{Binding ElemIds, Converter={StaticResource StringToVisibilityConverter}}"/>
                                        <Button Content="Открыть лог"
                                                Command="{Binding DataContext.OpenLogCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"
                                                Style="{StaticResource SecondaryButtonStyle}"
                                                Padding="10,6"
                                                Visibility="{Binding ElemIds, Converter={StaticResource StringToVisibilityConverter}}"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
